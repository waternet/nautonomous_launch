<launch>

    <!-- run pointcloud_to_laserscan node -->
    <node pkg="pointcloud_to_laserscan" type="pointcloud_to_laserscan_node" name="pointcloud_to_laserscan">

        <remap from="cloud_in" to="/sensor/scan/point_cloud"/>
        <remap from="scan" to="/base_scan"/>
        <rosparam>
            target_frame: velodyne # Leave disabled to output scan in pointcloud frame
            transform_tolerance: 0.01
            min_height: -26.8 # 100 * tan(-15 degrees)
            max_height: 26.8 # 100 * tan(15 degrees)

            angle_min: -1.5708 # -M_PI/2
            angle_max: 1.5708 # M_PI/2
            angle_increment: 0.00087 # M_PI/3600.0 because around 0.1 - 0.4 degrees horizontal accuracy
            scan_time: 0.05  # frequency is 20
            range_min: 0.1  
            range_max: 100.0
            use_inf: true

            # Concurrency level, affects number of pointclouds queued for processing and number of threads used
            # 0 : Detect number of cores
            # 1 : Single threaded
            # 2->inf : Parallelism level
            concurrency_level: 1
        </rosparam>

    </node>

</launch>